package at.dcv.w01;

import java.util.Scanner;  // import the Scanner class

import java.util.Arrays;

public class T04 {
    public static void main(String[] args) {
/*        double variableA;
        double variableB;

        Scanner scan = new Scanner(System.in);

        System.out.println("Gib die erste Zahl ein:");
        variableA = scan.nextDouble();

        System.out.println("Wollen sie Addition (+), Subtraktion (-), Multiplikation (*) oder Division (/)?");
        String bestätigung = new java.util.Scanner( System.in ).next();

        System.out.println("Gib die zweite Zahl ein:");
        variableB = scan.nextDouble();

        if (bestätigung.equals( "+") ) {
            System.out.println("Ergebnis = " + (variableA + variableB));
        }
        if (bestätigung.equals( "-") ) {
            System.out.println("Ergebnis = " + (variableA - variableB));
        }
        if (bestätigung.equals( "*") ) {
            System.out.println("Ergebnis = " + (variableA * variableB));
        }
        if (bestätigung.equals( "/") ) {
            System.out.println("Ergebnis = " + (variableA / variableB));
        }*/

//        String txt = "*";
//        int lineCount = 10 ;
//
//
//        for (int lineNumber = 1; lineNumber <= (lineCount / 2); ++lineNumber) {
//            for (int lineWidth = 1; lineWidth <= lineNumber; ++lineWidth) {
//                System.out.print(txt);
//            }
//            System.out.println();
//        }
//
//        for (int lineNumber = 1; lineNumber <= lineCount; ++lineNumber) {
//            for (int lineWidth = 1; lineWidth <= 1 + lineNumber- lineCount; ++lineWidth) {
//                System.out.print(txt);
//            }
//            System.out.println("_");
//        }

        int k = 0;

        while (k < 9) {

            int i = 0;
            int columns = 0;

            if (k <= 5) {
                columns = k;
                } else {
                columns = 9 - k;
            }
            }
        }



    }


